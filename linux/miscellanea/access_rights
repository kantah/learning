$ users - узнать какие пользователи есть сейчас в системе

$ less /etc/passwd  # узнать какие вообще есть пользователи
sys:x:3:3:sys:/dev:/usr/sbin/nologin  # первая запись: логин, третья: UID
petr:x:1000:1000:petr,,,:/home/petr:/bin/bash  # для системных служб UID < 1000, для пользователей >= 1000

$ groups <user>  # узнать группы текущего пользователя
$ groups  # узнать группы текущего пользователя

$ ls -l <путь>  # узнать права доступа

$ chmod [ugoa][+-][rwx] <путь>  # изменить права доступа
$ chmod 777 <путь>  # восьмеричный код

$ chown new_user:new_group <путь>  # изменить обладателя
$ chown new_user <путь>
$ chown :new_group <путь>

root (группа root)  # суперпользователь
sudo <действие>  # сделать от имени root

sticky bit(t) используется в основном для каталогов, чтобы защитить в них файлы. Из такого каталога пользователь может удалить только те файлы, владельцем которых он является. Примером может служить каталог /tmp, в который запись открыта для всех пользователей, но нежелательно удаление чужих файлов. Установка атрибута производится утилитой chmod.
