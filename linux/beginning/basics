ctrl+alt+t  # запуск терминала

ctrl+shift+t  # при открытом окне терминала - новая вкладка

pwd  # print working directory, вывести текущую директорию

ls  # list directory content, вывести содержимое директории

Общий вид команды:
команда опции аргументы
Например:
ls --all --human-readable -l /home  # ls - команда; --all --human-readable -l - опции: --all - все файлы и папки, включая скрытые, --human-readable - делает вывод более читабельным;
    -l - преобразует вывод в список; /home - путь, если не указан, то - текущая директория
- ставится перед короткими опциями, -- если в имени опции больше двух букв

У большинства опций есть две версии: длинная и короткая
Например ls --all --human-readable -l /home == ls -a -h -l /home

Минусы также можно сократить: ls -ahl /home

Внутри записи порядок опций произвольный: ls -ahl /home == ls -lha /home

ll == ls -la

man команда  # справка по команде (для выхода 'q')

clear  # очитить окно терминала

ctrl+l - перемещает курсор вниз, оставляя ранний ввод за пределами экрана

exit(ctrl+d)  # закрыть терминал

Перемещение.
Стрелка вверх, стрелка вниз - перемещение по введенным ранее командам
Стрелка вправо, стрелка влево - перемещение по команде
ctrl+a - в начало строки
ctrl+с - в конец строки

ctrl+shift+c - скопировать текст

ctrl+shift+v - вставить текст, выделенный текст можно скопипастить колесиком мыши

mkdir путь  # создать директорию

mkdir -p путь  # создать директорию и ее родителей

touch путь  # создать файл

cd путь  #  войти в директорию

rm путь  # удалить файл

rm -r путь  # удалить директорию

rm -rf путь  # удалить директорию с вложенными файлами и папками

cp путь_файл1 путь_файл2  # скопировать файл1 в файл2. Если вместо путь_файл2 будет путь в директорию, файл1 скопируется в нее под своим именем

cp -r путь_дир1 путь_дир2  # скопировать директорию 1 в директорию 2

mv путь1 путь2  # переместить директорию или файл
